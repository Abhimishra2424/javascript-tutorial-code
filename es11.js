// es2020 es11

// BigInt 
// is a built-in object that provides a way to represent
//  whole numbers larger than 2⁵³ — 1, which is the largest number in JS. 
// Javascript can now reliably represent beyond the Number primitive and not 
// just max out at 9007199254740992.
// A BigInt is created by adding n to the integer (ex. 5n) 
// or by calling BigInt().

// let oldnum = Number.MIN_SAFE_INTEGER;
// console.log(oldnum);

// const newnum = 9007199254740991n +10n;
// console.log(newnum);
// console.log(typeof newnum);